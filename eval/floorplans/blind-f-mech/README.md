# blind-f-mech

clear width between heaters is the ONLY north-south figure — a misread closes silently

The true 31 Aug silent shape: neither east-west wall carries a total, only the clear width between two 15" heaters. Reading the clear width as the wall length closes cleanly and builds a room 30" too small with no refusal anywhere — only the scorer would see it. The right take-off records the 15" + clear + 15" chain.

**BLIND CASE.** Generated by `python eval/gen-plans.py --blind2 407` — no takeoff fixture is emitted; the committed `takeoff.json` (if present) was transcribed from `input/photo.png` by an ISOLATED agent that never saw `truth.json` or this repo. Do not "fix" it — what it got wrong is the trial's data. Regenerating with the same seed reproduces the same truth.
