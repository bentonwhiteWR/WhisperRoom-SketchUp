# synthetic-jog

wall that returns (8-run jog) with a parts chain on the far wall

A north wall that jogs 2' down mid-run (six runs, two of them
the return), the opposite wall carrying the closing 8' + 4' = 12' chain, and
a door 12" from the jogged corner. Pass = built within 0.1" including both
jog vertices and the door jambs.

Generated by `eval/gen-plans.py` — regenerate with `python eval/gen-plans.py synthetic-jog`. Truth is authored in that file FIRST; the plan in `input/` is drawn from the truth, never the reverse.
