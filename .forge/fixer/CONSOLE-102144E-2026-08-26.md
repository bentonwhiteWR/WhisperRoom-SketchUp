# MDL 102144 E — build console, 2026-08-26 (v1.6.26)

Benton's own paste. **He also confirmed: "No neither 96144 or 102144 have the correct side
wall positions from the link."** So the mirror is real on both and comes through the LINK path.

## booth-from-link's own translation (RAW PACK)

    E0   40VNT            <- STDWL40 VNT      E0i  ENH 35.5VNT
    E1   16PanelSolid     <- STDWL16          E1i  ENH 11.5PanelSolid
    E2   40PanelSolid     <- STDWL40          E2i  ENH 35.5PanelSolid
    N0   40VNT            <- STDWL40 VNT      N0i  ENH 35.5VNT
    N1   40VNT            <- STDWL40 VNT      N1i  ENH 35.5VNT
    N2   16PanelSolid     <- STDWL16          N2i  ENH 11.5PanelSolid
    N3   40VNT            <- STDWL40 VNT      N3i  ENH 35.5VNT
    S0   RightWADoor      <- WA STDDRFRM R    S0i  ENH RightWADoor
    S1   31Panel          <- STDWL31          S1i  ENH 26.5Panel
    S2   16PanelSolid     <- STDWL16          S2i  ENH 11.5PanelSolid
    S3   40PanelSolid     <- STDWL40          S3i  ENH 35.5PanelSolid
    W0   40Panel2636WDO   <- STDWL40 WDO2636  W0i  ENH 35.5Panel2636WDO
    W1   16PanelSolid     <- STDWL16          W1i  ENH 11.5PanelSolid
    W2   40PanelSolid     <- STDWL40          W2i  ENH 35.5PanelSolid

    Door         Front (S0)
    Window       Left (W0)
    Ventilation  Right (E0), Back (N0), Back (N1), Back (N3)

**So the window IS in W0 and the portal agrees it belongs in W0.** The mirror is therefore NOT
"the window went into the wrong slot name" — consistent with the earlier finding that slot 0
sits on the same physical end in both files across all 200 walls. The question is where W0
physically LANDS, or which way the run is walked.

## FACING column — the new evidence

    N0  40VNT          Y+ OUT·floor hangs 1.000     N2  16PanelSolid   Y- IN
    N1  40VNT          Y+ OUT·floor hangs 1.000     N3  40VNT          Y+ OUT·floor hangs
    S0  RightWADoor    Y+ IN·bulk                   S1  31Panel        Y+ IN
    S2  16PanelSolid   Y+ IN                        S3  40PanelSolid   Y+ IN
    E0  40VNT          X+ OUT·floor hangs           E1  16PanelSolid   X- IN
    E2  40PanelSolid   X- IN
    W0  40Panel2636WDO X+ IN·bulk                   W1  16PanelSolid   X+ IN
    W2  40PanelSolid   X+ IN

**Note the W wall is internally consistent — W0, W1, W2 are all `X+ IN`, and the window panel
faces the same way as its neighbours.** That is evidence AGAINST the earlier hypothesis (A),
which was that the window panel sits in the X-width family and is missing the half turn that
`IEP_VENT_YAW` gives the vents. Re-test that hypothesis against this table before reusing it.

Contrast the N and E walls, where the vents read `OUT` and the solids read `IN` — that IS the
two-family split, but it is marked `·floor hangs` / `·bulk`, i.e. deliberate and measured.

## Other facts from the same run

- `PANEL` column: every **outer** part found its slab (`1.0000`). Every **inner** `ENH` panel
  reads `NOT FOUND` except the door (`ENH RightWADoor`, 1.0000).
- The S wall rebalanced (`RightWADoor` 49 wide into a 40 slot, seal shifted +9.000) on both
  shells. That is the WA door and looks intended, but it is the one wall whose run moved.
- 17 parts do not measure their slot: the `+0.2500` on every `ENH 35.5VNT`, `+0.1250` on every
  `ENH 35.5PanelSolid`, `+0.1169` on `ENH 26.5Panel`, and the unmeasured room-proud warnings
  for 11.5 / 26.5 / 35.5.

## Tray — the v1.6.25 detector ABSTAINED, and said so

    ENH 10242CL SIDE  ... NO ORIENTATION CUE: both ends carry similar area (4472 low, 4284 high)
    ENH 10242CL CTR   ... NO ORIENTATION CUE: both ends carry similar area (4368 low, 4284 high)
    ENH 10218CL CTR   ... NO ORIENTATION CUE: both ends carry similar area (1872 low, 1836 high)

Benton says the tray must point DOWN and it is pointing UP. The mouth tell cannot separate
4472 from 4284 — a 4% difference. **The detector is working as designed and honestly reporting
that it cannot see; it is not misfiring.** Fixing this needs real face-level data for the ENH
parts, which does not exist: `_face-levels.tsv` has ZERO `ENH` rows. Run `scripts/probe-levels.rb`
over the folder with an EMPTY filter to generate it.
